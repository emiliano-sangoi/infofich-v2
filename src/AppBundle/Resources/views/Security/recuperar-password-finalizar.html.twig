{% extends '::base.html.twig' %}

{% set full_width = false %}

{% block navbar %}
    {% include 'navbar-simple.html.twig' %}
{% endblock %}

{% block navbar2 %}
    {% embed 'navbar2.html.twig' %}
        {% block navbar2_actions %}
        {% endblock %}
    {% endembed %}
{% endblock %}


{% block content %}

    <div class="">
        <div class="px-lg-5">

            <div class="my-3">
                {% include '::notificaciones.html.twig' %}
            </div>

            <div class="bg-light px-4 py-3">
                <h3 class="font-weight-bold">
                    Recuperar contraseña
                </h3>
                <p class="mt-4 mb-1">
                    Para finalizar el proceso de recuperaci&oacute;n ingrese el c&oacute;digo de verificaci&oacute;n recibido por correo electrónico y su nueva contraseña.
                </p>
                <div class="my-4">

                    {{ form_start(form) }}

                    {% if form.vars.errors is not empty %}
                        <div class="">
                            {{ form_errors(form) }}
                        </div>
                    {% endif %}

                    <div class="row mb-4">
                        <div class="col-12">
                            {{ form_label(form.stringRecupPwd) }}
                            {{ form_widget(form.stringRecupPwd) }}
                            {{ form_errors(form.stringRecupPwd) }}
                        </div>
                    </div>
                    <div class="row mb-5">
                        <div class="col-12 col-md-6">
                            {{ form_label(form.password) }}
                            {{ form_widget(form.password) }}
                            {{ form_errors(form.password) }}
                        </div>
                        <div class="col-12 col-md-6">
                            {{ form_label(form.password2) }}
                            {{ form_widget(form.password2) }}
                            {{ form_errors(form.password2) }}
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-12 col-md-6">
                            <button type="submit" class="btn btn-primary">
                                Confirmar cambios
                            </button>
                        </div>
                        <div class="col-12 col-md-6 text-right">
                            <a href="{{ path('homepage') }}" class="btn btn-primary">
                                Cancelar
                            </a>
                        </div>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}