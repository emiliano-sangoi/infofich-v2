{% extends '::base.html.twig' %}

{% set full_width = false %}

{% block navbar %}
    {% include 'navbar-simple.html.twig' %}
{% endblock %}

{% block navbar2 %}
    {% embed 'navbar2.html.twig' %}
        {% block navbar2_actions %}
        {% endblock %}
    {% endembed %}
{% endblock %}


{% block content %}
    <div class="">
        <div class="px-lg-5">
            <div class="my-3">
                {% include '::notificaciones.html.twig' %}
            </div>

            <div class="bg-light px-4 py-3">
                <h3 class="font-weight-bold">
                    Recuperar contraseña
                </h3>
                <p class="mt-4 mb-1">
                    Se ha enviado un correo electrónico a la dirección configurada en la cuenta de usuario de <b>{{ usuario.username }}</b> con instrucciones para restablecer su contraseña.
                </p>
                <p>
                    En caso de que el correo no llegue o tenga algún otro inconveniente escriba un correo a la dirección <b><i>{{ gidis_email }}</i></b> detallando el problema.
                </p>
                <div class="mt-3 mt-lg-4">
                    <a href="{{ path('homepage') }}" class="btn btn-primary">
                        Ir al inicio
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}