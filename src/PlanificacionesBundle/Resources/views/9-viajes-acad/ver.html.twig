{% set page_id = 9 %}

{% extends "PlanificacionesBundle::planif-base.html.twig" %}

{% block tab_content %}

    {% include "PlanificacionesBundle:9-viajes-acad:form.html.twig" %}

    <div class="form-row mt-5">
        <div class="form-group col-md-10">
            {% embed '::botonera.html.twig' %}

                {% block left %}
                    {% if planificacion.puedeEditarse() %}
                        <a href="{{ path('planif_viaje_editar', { id : viaje.id }) }}"
                           class="btn btn-success">
                            <i class="fa fa-edit"></i>&nbsp;Editar
                        </a>
                    {% endif %}
                {% endblock %}

                {% block right %}
                    {% if planificacion.puedeEditarse() %}
                        {{ form_start(delete_form) }}
                        <button role="submit" class="btn btn-danger confirmar-borrado"
                                data-msg='¿Esta seguro que desea borrar esta viaje?'>
                            <i class="fa fa-trash"></i>&nbsp;Borrar
                        </button>
                        {{ form_end(delete_form) }}
                    {% endif %}

                    <a href="{{ path('planif_viaje_index', { id: viaje.planificacion.id }) }}"
                       class="btn btn-outline-secondary">
                        Ir a viajes
                    </a>
                {% endblock %}

            {% endembed %}
        </div>
    </div>

{% endblock %}


{% block javascripts %}
    {{ parent() }}

{% endblock %}